/**
 * App version — chỉ cần sửa APP_VERSION ở đây mỗi khi deploy.
 * CSS và JS sẽ tự động được cache-bust theo giá trị này.
 *
 * Quy ước versioning: MAJOR.MINOR.PATCH
 *   MAJOR — thay đổi lớn về tính năng / cấu trúc
 *   MINOR — thêm tính năng mới / UI cải tiến
 *   PATCH — sửa lỗi nhỏ / chỉnh wording
 */
const APP_VERSION = '1.2.0';
